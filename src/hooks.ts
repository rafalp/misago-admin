import React from "react"
import { MAIN_NAV, ROUTES } from "./site"

const EXTRA_COMPONENTS: Record<string, Array<React.ComponentType>> = {
  TEST: [],
}

export { EXTRA_COMPONENTS, MAIN_NAV, ROUTES }
